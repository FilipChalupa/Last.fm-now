extends layouts/default

append vars
	- var page = 'screen'
	- var description = 'Shows currently playing track.'

block content

	include parts/track
	include parts/user

	script.
		initComponents.push({
			name: 'checker',
			place: 'body',
			data: {
				trackWrap: '#tracks',
				userWrap: '#user',
				statesWrap: '#states'
			}
		})


